<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.mingempty.meta.data.repository.mapper.AuthorizationMapper">
    <delete id="deleteChange">
        delete base
        from t_meta_data_authorization base
        inner join t_meta_data_change_authorization chg on chg.entry_code = base.entry_code
        and chg.authorization_type = base.authorization_type
        and chg.authorization_code = base.authorization_code
        <where>
            <include refid="top.mingempty.meta.data.repository.mapper.ChangeAuthorizationMapper.base-query-param"/>
            and chg.delete_status = 1
        </where>
    </delete>
    <insert id="transferChange">
        insert into t_meta_data_authorization(
        authorization_id
        , entry_code
        , authorization_type
        , authorization_code
        , create_time
        , update_time
        , create_operator
        , update_operator)
        select base.authorization_id
        , base.entry_code
        , base.authorization_type
        , base.authorization_code
        , base.create_time
        , base.update_time
        , base.create_operator
        , base.update_operator
        from t_meta_data_change_authorization base
        <where>
            <include refid="top.mingempty.meta.data.repository.mapper.ChangeAuthorizationMapper.base-query-param"/>
            <include refid="top.mingempty.meta.data.repository.mapper.ChangeAuthorizationMapper.max-query"/>
            and base.delete_status = 0
        </where>
        on duplicate key update
        authorization_id=values(authorization_id)
        , update_time=values(update_time)
        , update_operator=values(update_operator)
    </insert>
    <select id="queryCount" resultType="java.lang.Long">
        select count(authorization_id)
        from t_meta_data_authorization base
        <where>
            <include refid="top.mingempty.meta.data.repository.mapper.ChangeAuthorizationMapper.base-query-param"/>
        </where>
    </select>

    <select id="query" resultType="top.mingempty.meta.data.repository.model.po.AuthorizationPo">
        select base.*
        from t_meta_data_authorization base
        <where>
            <include refid="top.mingempty.meta.data.repository.mapper.ChangeAuthorizationMapper.base-query-param"/>
        </where>
        order by base.entry_code,base.authorization_type,base.authorization_code
        <include refid="top.mingempty.BaseMapper.limit"/>
    </select>
</mapper>
